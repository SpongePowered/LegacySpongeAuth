play.crypto.secret          =   "changeme"
play.crypto.secret          =   ${?APPLICATION_SECRET}
play.i18n.langs             =   [ "en", "en-US" ]
play.http.session.maxAge    =   2419200 // 4 weeks (seconds)

sso.debug                   =   true // disable for production
sso.secret                  =   "changeme"
sso.secret                  =   ${?SPONGE_SSO_SECRET}
sso.username.minLen         =   3
sso.username.maxLen         =   20
sso.password.minLen         =   10
sso.password.maxLen         =   60
sso.password.saltLogRounds  =   10
sso.maxAge                  =   600000 // 10 minutes (millis)

db.timeout = 10000 // 10 seconds (millis)

mail.username           =   ${SPONGE_SSO_EMAIL_USER}
mail.email              =   ${SPONGE_SSO_EMAIL}
mail.password           =   ${SPONGE_SSO_PASSWORD}
mail.smtp.host          =   ${SPONGE_SSO_SMTP_HOST}
mail.smtp.port          =   ${SPONGE_SSO_SMTP_PORT}
mail.confirm.maxAge     =   600000                      // 10 minutes (millis)
mail.transport.protocol =   "smtps"                     // The outgoing transport protocol
mail.interval           =   30000                       // 30 seconds (millis)

// Custom properties to be added to the Properties object that is passed to
// the JavaMail session. These might vary from SMTP server to server. This
// configuration is set up for GMail with "allow less secure apps" enabled in
// the user settings.
mail.properties = {
    "mail.smtps.host"=${SPONGE_SSO_SMTP_HOST}
    "mail.smtp.port"=${SPONGE_SSO_SMTP_PORT}
    "mail.smtp.socketFactory.class"="javax.net.ssl.SSLSocketFactory"
    "mail.smtp.socketFactory.fallback"=false
    "mail.smtp.socketFactory.port"=${SPONGE_SSO_SMTP_PORT}
    "mail.smtp.smtps.auth"=true
    "mail.smtps.quitwait"=false
}

// Slick configuration
// Default (main) database
slick.dbs.default.driver        =   "slick.driver.PostgresDriver$"
slick.dbs.default.db.driver     =   "org.postgresql.Driver"
slick.dbs.default.db.url        =   "jdbc:postgresql://localhost/spongesso"
slick.dbs.default.db.user       =   "root"
slick.dbs.default.db.password   =   ""
slick.dbs.default.db.password   =   ${?JDBC_DATABASE_PASSWORD}

// Test database
slick.dbs.test.driver        =   "slick.driver.PostgresDriver$"
slick.dbs.test.db.driver     =   "org.postgresql.Driver"
slick.dbs.test.db.url        =   "jdbc:postgresql://localhost/test"
slick.dbs.test.db.user       =   "root"
slick.dbs.test.db.password   =   ""
slick.dbs.test.db.password   =   ${?JDBC_TEST_DATABASE_PASSWORD}

// Sponge settings
sponge.logo = "https://forums-cdn.spongepowered.org/uploads/default/original/2X/7/77fa5f82289385db14561fac384ddea2a84a0070.png"
sponge.icon = "https://forums-cdn.spongepowered.org/uploads/default/original/2X/9/9ba706a80e45cf427617525ee2a19fad7bb6b109.png"
